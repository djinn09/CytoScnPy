# cargo-deny configuration for CytoScnPy
# https://embarkstudios.github.io/cargo-deny/

# Root options
[graph]
targets = []
all-features = true

# =============================================================================
# LICENSES - Define which licenses are allowed/denied
# =============================================================================
[licenses]
# The confidence threshold for detecting a license from license text.
confidence-threshold = 0.8

# List of explicitly allowed licenses
allow = [
    "MIT",
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "Unicode-DFS-2016",
    "Unlicense",
    "CC0-1.0",
    "MPL-2.0",
    "Unicode-3.0",
    "LGPL-3.0-only",                  # malachite crates via rustpython-parser
]

# =============================================================================
# BANS - Forbid specific crates or versions
# =============================================================================
[bans]
# Lint level for multiple versions of the same crate
multiple-versions = "warn"

# List of crates to deny (security concerns, deprecated, etc.)
deny = [
    # Example: { crate = "openssl", use-instead = "rustls" }
]

# Certain crates/versions that are allowed (skip bans check)
skip = []

# =============================================================================
# ADVISORIES - Security vulnerability checking
# =============================================================================
[advisories]
# List of advisory IDs to ignore (with rationale)
ignore = [
    # =====================================================================
    # Unmaintained unic-* crates from rustpython-parser
    # These are transitive dependencies we can't control directly.
    # rustpython-parser 0.4.0 (latest) still depends on these crates.
    # Will be resolved when rustpython-parser removes these dependencies.
    # Tracking: https://github.com/RustPython/Parser/issues
    # =====================================================================
    "RUSTSEC-2025-0075", # unic-char-range
    "RUSTSEC-2025-0080", # unic-common  
    "RUSTSEC-2025-0081", # unic-char-property (corrected ID)
    "RUSTSEC-2025-0090", # unic-emoji-char
    "RUSTSEC-2025-0100", # unic-ucd-ident
    "RUSTSEC-2025-0098", # unic-ucd-version
]

# =============================================================================
# SOURCES - Define trusted sources for crates
# =============================================================================
[sources]
# Lint level for crates from sources not in the allow list
unknown-registry = "deny"

# Lint level for crates from git sources not in the allow list
unknown-git = "warn"

# List of allowed registries
allow-registry = ["https://github.com/rust-lang/crates.io-index"]

# List of allowed git sources
allow-git = []
